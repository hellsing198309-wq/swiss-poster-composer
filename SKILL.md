---
name: swiss-poster-composer
description: 将每张上传照片独立转译为 3:4 竖版、上下约 1:1 分区的 Swiss International Style 摄影海报；保留原图主体，并把其结构抽象为严格的 Swiss Grid 几何语言。
metadata:
  short-description: 从单张照片生成严格 Swiss Grid 双段式摄影海报
---

# Swiss Poster Composer

## 目标

把一张真实上传照片转译为一张展览级的「上半部摄影、下半部几何抽象」海报。整体气质遵循 Swiss International Typographic Style：冷静、理性、克制、精确，接近现代摄影出版物与国际主义艺术展海报。

核心参照：Josef Müller-Brockmann、Armin Hofmann、Swiss Grid System、现代建筑档案摄影与 Editorial Photography。

## 输入与输出

- 输入：一张上传照片。
- 输出：一张独立的 3:4 竖版摄影海报。
- 如果用户上传多张照片，必须逐张独立处理：每张照片生成一张完整海报，不合并照片，不共用同一套抽象图形，不把多张照片拼成一张海报。
- 若没有提供可用照片，不要凭空替换成另一张同风格照片；先要求用户提供照片。

## 不可偏离的硬约束

1. **保留原图主体**：上半部必须使用当前上传照片作为主体来源，并保留其可识别的主体、空间关系与环境氛围。不得把原照片替换成另一个建筑、物体或场景，也不得只生成一张与原图无关的风格化海报。
2. **固定画幅**：最终画面为 3:4 竖版；上下区域严格约 1:1。上半部为摄影，下半部为抽象几何转译。
3. **结构对应**：下半部的几何关系必须来自上半部主体的轮廓、透视、重复节奏、阴影、比例或方向。抽象不是随机装饰，不能随意添加与原图无关的形状。
4. **网格优先**：全画面共用同一套纵横网格与基线逻辑。矩形、圆形、半圆、辅助线、文字和分区边界都应对齐网格；避免自由漂浮和无规律的拼贴。
5. **独立分析**：每张照片单独分析主体、几何特征和核心颜色。多张照片之间可以保持风格一致，但不得复制同一张照片的抽象方案。
6. **克制表达**：不使用卡通化、过度滤镜、无意义渐变、装饰性边框、复杂纹理、霓虹高饱和配色或大量文字。

## 参考效果标准：Nordic Horizon 版式

当用户提供了类似 `NORDIC HORIZON` 的参考成品时，以该成品作为视觉基准，而不是只套用“瑞士风”关键词。参考成品的关键不是题材，而是以下完整系统：

- 画布为 3:4 竖版，外圈保留米白纸张边距，内侧有清晰但极细的框线。
- 上半部约占一半高度，几乎完整保留原照片；不要把照片缩成小卡片，也不要用新的 AI 场景替换原照片。
- 下半部以米白为地面，叠加低对比度的纵横模块网格、主体轮廓的几何色块、极细圆弧或辅助线，以及少量与主体有关的线性细节。
- 下半部应像一张由原照片“推导出来”的展览图：照片中的山峰、地平线、月亮、枝条、桥梁、楼梯或立面节奏，都要在几何区中留下可识别的对应关系。
- 左侧设置清晰的标题与元信息区；底部左侧可放 3–4 个小型色板，底部右侧放年份。它们都要对齐网格，不得成为装饰性贴纸。
- 参考图的色彩是低饱和的深蓝、灰蓝、浅蓝、粉雾色、米白与黑色。实际输出仍应从当前照片取色，不要固定套用蓝粉配色。
- 参考样本覆盖城市观察、舞者、老爷车与情侣、黑猫、城堡建筑和山景，说明该 Skill 的重点是“主体到形式”的转译方法，而不是固定题材或固定配色。

这些是版式与层级规则，不是固定素材。面对山景时可以用层叠山峰、弧线和枝条；面对建筑、桥梁或人物时，应替换成当前主体对应的轮廓和节奏。

## 版式系统

### 画幅与分区

- 采用 3:4 竖版构图。
- 画面上下分成两个约等高的区域，分界线清晰但不过度装饰。
- 上下区域使用同一套模块化网格，使摄影与抽象之间形成连续的结构关系。
- 留白应充分；边距、分栏和基线保持精确、稳定、可重复。

### 上半部：原图摄影

- 完整保留原照片的主体与环境氛围；只做必要的构图适配，不裁切核心内容。
- 根据主体选择高级建筑、风景、静物或人物 Editorial Photography 与美术馆档案摄影的处理标准；不要为了“建筑感”强行改变非建筑照片。
- 降低不必要的颜色噪声与饱和度，控制高光，避免死白和过曝。
- 保持真实摄影质感、空间层次和主体的可识别性，不把照片处理成插画、3D 渲染或全新的虚构场景。
- 可以统一白平衡、对比度和影调，但不要改变主体的真实比例、朝向或关键结构。

### 下半部：几何抽象转译

从上半部主体中提取最核心的结构关系，再用纯粹的平面语言重建：

- 大面积纯色几何块
- 精确矩形
- 圆形或半圆
- 极细辅助线
- 模块化网格
- 等分、比例或接近黄金分割的数学关系

转译时优先保留主体的真实比例与方向，例如：

- 建筑立面重复的竖向节奏 → 等距矩形或竖线模块
- 桥梁、楼梯或道路的透视方向 → 对齐的斜线、梯形或阶梯形块面
- 门洞、圆窗或拱形结构 → 圆形、半圆或负形
- 阴影与明暗分界 → 大面积黑白或低饱和色块
- 山脊、屋顶或地平线 → 层叠三角形、折线或连续的斜向色块
- 月亮、圆窗或远处光点 → 小圆形；拱顶、轨迹或视野边界 → 极细圆弧
- 枝条、栏杆或线性纹理 → 少量精确的细线，不画成装饰插图

删除所有非必要细节，但让观者仍能回看上半部并理解抽象图形的来源。抽象区不应制造新的主体、人物或场景。允许保留一层极轻的、从照片色彩提取的低对比度色调场；禁止厚重的装饰性渐变、发光效果和 3D 阴影。

## 色彩系统

- 从当前照片中提取 2–4 个核心颜色，用于抽象区的几何块、线条或局部强调。
- 统一加入米白 / Off-white 与灰黑 / Charcoal 作为背景和中性色。
- 总色数控制在 5–6 种以内；除非原图本身需要，不使用艳丽撞色。
- 颜色应服务于结构层级：大色块负责分区，深色负责锚定，少量强调色负责建立视觉焦点。
- 颜色应同时连接上下两区：照片中的主色在抽象区出现，但要降低饱和度并转成平面色块；不要让下半部突然变成另一套配色。

## 文字系统

文字是辅助信息，不得盖住照片主体或削弱抽象结构。

- 可使用固定范式：`OBJECT / FORM / STUDY 01 / 2026`
- 或根据当前照片生成一个 1–3 个单词的简洁英文标题，例如 `LIGHT / VOLUME`、`STRUCTURE 01`。
- 使用类似 Helvetica、Akzidenz-Grotesk、Neue Haas Grotesk 的现代无衬线体系。
- 文字左对齐，遵循网格基线；主标题、编号 / 年份和辅助小字应有明确层级。
- 文字极少，避免长句、花体和装饰性字体。
- 推荐版式层级：下半部左上为 1–2 行英文标题；标题下方是一条短横线；再下方为 `OBJECT / FORM / STUDY 01 / 2026` 等元信息；底部左侧为色板，底部右侧为年份。
- 如果生成工具无法可靠渲染字母，不要输出乱码；宁可省略文字，或保留清晰留白供后续排版。

## 执行流程

1. 只分析当前这张照片：确认主体、环境、透视方向、重复节奏、阴影关系与 2–4 个核心颜色。
2. 建立 3:4 画布和约 1:1 的上下分区，先确定统一网格、边距、分栏和基线。
3. 将原照片放入上半部，做克制的主体适配型摄影调色；检查主体是否仍然可识别，是否保留环境氛围。
4. 从照片的结构特征提取抽象规则，并在下半部以几何块、圆 / 半圆、细线和网格重新构建。
5. 在下半部左侧建立标题、短横线和元信息层级；按需在底部放置色板与年份，所有元素对齐网格。
6. 进行最终检查：画幅比例、上下比例、主体保留、结构对应、网格对齐、色彩数量、文字清晰度、色板与年份位置，以及整体留白。

## 追加修改指令

用户可以在本 Skill 基础上追加以下类型的修改：

- 指定标题文字或编号
- 指定从原图提取的主色倾向
- 提高或降低抽象程度
- 调整照片与抽象区的明暗关系
- 指定是否显示文字

追加修改只改变用户明确点名的部分，其余硬约束保持不变，尤其是：独立处理每张照片、保留原图主体、3:4 画幅、上下约 1:1、结构对应和 Swiss Grid。

## 可直接调用的提示词

```text
请将这张上传照片独立制作成一张 Swiss International Style 瑞士国际主义摄影海报。

每张上传照片必须单独生成一张完整海报；不要合并照片，也不要让不同照片共用同一套抽象图形。

画面采用 3:4 竖版，上下区域严格保持约 1:1，并在全画面使用统一的 Swiss Grid System。

【上半部分｜原图摄影】
完整保留当前上传照片的主体、空间关系与环境氛围。摄影部分采用与主体匹配的高级建筑、风景、静物或人物 Editorial Photography 与美术馆档案摄影标准。降低不必要的颜色噪声，控制饱和度与高光，让照片呈现冷静、理性、克制、精确的国际主义气质。不要把原照片替换成另一种建筑、物体、场景或插画。

【下半部分｜结构抽象】
只从上半部主体的轮廓、透视、重复节奏、阴影、比例和方向中提取结构，并转译为大面积纯色几何块、精确矩形、圆形、半圆、极细辅助线、模块化网格和数学比例关系。参考 `NORDIC HORIZON` 的完整层级：米白抽象地面、主体轮廓色块、低对比度网格、少量圆弧 / 线性细节、左侧标题与元信息、底部色板和右下年份。抽象图形必须与原照片存在可识别的结构对应，不得随机拼贴或添加无关主体。

【文字】
文字极少，可使用：OBJECT / FORM / STUDY 01 / 2026；或根据当前照片生成一个 1–3 个单词的简洁英文标题。使用类似 Helvetica、Akzidenz-Grotesk、Neue Haas Grotesk 的现代无衬线字体，左对齐并遵循网格基线。无法可靠渲染字母时不要输出乱码，可省略文字。

【色彩】
从当前照片中提取 2–4 个核心颜色，并加入米白与灰黑作为基础色。全图控制在 5–6 种颜色以内。

整体感觉：Müller-Brockmann、Swiss Design、现代摄影出版物、低饱和北欧景观画册、国际主义艺术展海报、极度理性、干净、精准、高级。

最终检查：当前照片主体仍可识别；上、下约 1:1；抽象区来源于当前照片；下半部有明确标题 / 元信息 / 色板 / 年份层级；所有元素遵循统一网格；没有随机几何、厚重渐变、卡通化、无关场景或乱码文字。
```
